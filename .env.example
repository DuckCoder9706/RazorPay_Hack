# Razorpay TEST-MODE keys (Dashboard → Settings → API Keys). Copy to .env; never commit .env.
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxxxxxxxxxx

# Anthropic (LLM lives OUTSIDE the scored path — narration/dunning copy only, temp 0 + cache)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# Deterministic run controls
SONIC_SEED=42
SONIC_DB_PATH=data/sonic.db
